using UnityEngine;
using System.Collections;

public class ChangeLabel : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //gameObject.GetComponent<UILabel>().text = Global_Variable.item_num.ToString();
	}
}
